"use client";

// Layout vazio para o dashboard - não usa o layout padrão do sistema
export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}

